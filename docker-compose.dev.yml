services:
  postgres:
    image: postgres:16-alpine
    container_name: maxim-pg-dev
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - maxim-postgres-data-dev:/var/lib/postgresql/data
    networks:
      - maxim-dev-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: maxim-pgadmin-dev
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD}
    ports:
      - "${PGADMIN_PORT}:8080"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
     - maxim-dev-network

volumes:
  maxim-postgres-data-dev:
    name: maxim-postgres-data-dev

networks:
  maxim-dev-network:
    name: maxim-dev-network
    driver: bridge
